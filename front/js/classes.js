// Used to created an object with product information
export class item {
    constructor( id, color, quantity ) {
        this.id = id,
        this.color = color,
        this.quantity = quantity
    }
}